/**
 * Re-export para manter compatibilidade com imports existentes.
 * Implementação movida para TasksTable/ (decomposição Sprint 3.9+).
 */
export { TasksTableView } from './TasksTable'
export type { TableSort, ColFilter } from './TasksTable'
